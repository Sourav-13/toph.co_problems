#include <bits/stdc++.h>
using namespace std;
int main()
{
    int x = 182, y = 243;
    string str1 = to_string(x), str2 = to_string(y);
    int size;
    if (str1.size() >= str2.size())
    {
        size = str1.size();
    }
    else
    {
        size = str2.size();
    }
    for (int i = 0; i < size; i++)
    {
        int x = int()
        int y =
    }

    return 0;
}